(function ($, undefined) {
    //扩展对象
    $.fn.combogrid.extensions = {};

    //扩展属性
    var defaults = $.fn.combogrid.extensions.defaults = {

    };

    //扩展方法
    var methods = $.fn.combogrid.extensions.methods = {

    };

    //扩展原生的属性或者方法
    $.extend($.fn.combogrid.defaults, defaults);
    $.extend($.fn.combogrid.methods, methods);

})(jQuery);